# digit-vision-ai
DigitVision AI is designed to recognize handwritten digits (0–9) from images. It uses a CNN trained on the MNIST dataset and includes preprocessing steps to handle real-world input variations such as scaling, noise, and misalignment.
